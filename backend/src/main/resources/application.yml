spring:
  application:
    name: ams-backend
  profiles:
    active: dev

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never

logging:
  level:
    com.ams: DEBUG
    org.springframework.security: DEBUG
